name: hello Github Actions

on:
    push:
        branches:
          - dev

jobs:
  say-hello:
      runs-on:  ubuntu-latest
      steps:
        - name: display log message
          run: echo "Github Action is working correctly"